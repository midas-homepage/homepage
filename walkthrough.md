# MIDAS Lab 홈페이지 개선 결과서 (Walkthrough)

본 문서는 MIDAS Lab 연구실 웹사이트의 전반적인 레이아웃, 디자인, 기능 연동 개선 사항에 대한 최종 작업 결과를 설명합니다. 본 개선 작업을 통해 웹사이트의 심미성(Rich Aesthetics)과 사용성을 전문 학술 기관 수준으로 향상시켰습니다.

---

## 주요 개선 내용

### 1. 관리자 모드 구성원 사진 파일 업로드 및 실시간 서버 저장 [NEW]
- **로컬 서버 업로드 API 신설**: `dev_server.py`에 `/api/upload-member-photo` POST 엔드포인트를 구현하였습니다. 클라이언트가 JSON으로 전송한 base64 이미지 데이터를 파싱하여 로컬 `images/people/` 폴더에 즉시 덮어쓰기 저장합니다. 이 과정에서 파일명 경로 탈출 공격(Directory Traversal)을 방지하도록 `os.path.basename`을 통해 안전하게 파일명을 필터링합니다.
- **실시간 미리보기 및 파일 선택 필드**: `index.html` 모달 구조 내에 파일 업로드(`<input type="file">`) 제어 컨트롤과 둥근 프로필 실시간 미리보기(Preview) 상자를 추가했습니다. 관리자가 모달을 열면 현재 등록된 사진(없으면 이름 이니셜 플레이스홀더)을 띄우고, 새로운 이미지 파일을 선택하는 즉시 Live 미리보기로 전환됩니다.
- **Canvas 기반 압축 및 자동 파일명 생성**: ID를 타이핑하면 `[id].jpg` 파일명이 자동 입력되며, 사진 저장 시 Canvas API를 통해 최대 크기를 `500px`로 스케일 다운 및 `JPEG 0.85` 비율로 고품질 경량화 압축(약 30~50KB)을 가하여 서버 스토리지 및 네트워크 전송을 극대화합니다.
- **캐싱 우회 파라미터 (Cache Busting)**: 브라우저가 동일 파일명 이미지의 변경을 바로 알아차리도록, 구성원 데이터를 수정할 때마다 고유 타임스탬프 버전(`member.version`)을 갱신하고, 메인 목록 렌더링 시 이미지 경로 뒤에 쿼리(`?v=[version]`)를 결합하여 새로 고침 없이도 변경된 사진이 실시간으로 즉시 출력되도록 보장했습니다.

### 2. 구성원 이메일 주소 토글 노출 (Mail Toggle)
- **메일 주소 숨김 연동**: 교수님을 제외한 연구실 모든 구성원(재학생 및 졸업생)의 카드 내 메일 주소를 기본적으로 화면에서 가리도록 구성했습니다.
- **클릭 토글 인터랙션**: 메일 주소 대신 세련된 이메일 봉투 모양의 SVG 아이콘 버튼이 나타나며, 사용자가 이 아이콘을 클릭할 때에만 메일 주소 텍스트가 노출되도록 `app.js`에서 클릭 이벤트를 연동했습니다. 다시 한 번 클릭하면 메일 주소가 다시 가려집니다.
- **부드러운 페이드인 모션**: 이메일 주소가 나타날 때 딱딱하게 켜지지 않고, 왼쪽에서 오른쪽으로 5px 부드럽게 밀려오며 페이드인되는 키프레임 애니메이션(`fadeInEmail`)을 추가하여 고품격 UX를 제공합니다.

### 3. Publications(논문 목록) 페이지 고도화 및 인물 하이라이트
- **글로벌 순번 자동 배지화**: 전체 논문 개수 $N$을 기준으로 역순 정렬된 목록에서 최신 논문부터 `#N`에서 `#1`까지의 글로벌 고유 번호를 동적으로 계산하여 연도 배지(`.pub-year-badge`) 내에 세로 정렬 형태로 렌더링되도록 수정했습니다. 필터나 검색 시에도 논문의 고유 번호는 변하지 않고 안전하게 보존됩니다.
- **저자 상세 역할 위첨자 표기**: 저자명 목록 문자열에서 공동 제1저자 기호(`†`)와 교신저자 기호(`*`)를 파싱하여 마우스 호버 시 도움말 툴팁("Equal Contribution", "Corresponding Author")이 활성화되는 `<sup>` 태그로 자동 치환하여 출력합니다.
- **연구원 자동 매칭 및 강조**: 저자명에서 기호를 제거한 순수 이름(`cleanName`)을 추출하고, 대소문자/공백/하이픈을 무시한 정규화 비교를 거쳐 연구실 인물 데이터베이스(`members` - 재학생 및 졸업생 전체 포함) 및 지도교수명과 비교하여 일치하는 경우, 교수님과 동일하게 굵은 폰트와 밑줄 스타일(`.author-highlight`)이 일관성 있게 자동 부여됩니다.
- **모바일 반응형 레이아웃**: 모바일 모드(768px 이하)에서 세로 배치 배지가 깨지지 않고 가로 방향 한 줄로 깔끔하게 정렬되도록 미디어 쿼리를 추가했습니다.

### 4. Overview(메인화면) NEWS & HIGHLIGHTS 롤링 배너 자동화
- **실시간 데이터 연동**: 메인 화면의 뉴스 롤링 배너를 하드코딩된 정적 문구에서 탈피하여, 게시판 데이터베이스(`localStorage`의 `midas_board_posts` 또는 `data/posts.json` 파일)의 `"news"` 카테고리에서 가장 최신 소식 3개를 동적으로 연동하도록 변경했습니다.
- **강건성(Robustness) 확보**: 뉴스가 3개 미만인 경우에도, CSS 무한 롤링 키프레임 애니메이션(12초 주기로 0px, -24px, -48px, -72px 단위 변환)이 끊기거나 빈 여백이 보이지 않도록 배열 크기에 따른 복사 배열 채우기 로직을 결합했습니다.
- **seamless 무한 루프 구현**: 3개의 최신 소식 리스트 끝부분에 첫 번째 글의 클론 노드를 동적으로 덧붙여(Clone Element), 롤링 애니메이션이 끝난 후 다시 첫 번째 뉴스로 점프할 때 툭 끊기는 현상 없이 부드러운 순환 롤링 효과를 완성했습니다.

### 5. 타이포그래피 정밀 조정 (한글 sans-serif 복구 및 영문 국소 serif화)
- **한글 타이포그래피 원상복구**: 한국어 텍스트는 원래 디자인인 sans-serif로 유지하고,
- **브라우저 HTTP 캐시 버스팅**: 빌드가 완벽히 끝난 이후에도 브라우저가 디스크 캐시에 보관 중인 옛날 JSON 데이터베이스 파일(`posts.json`, `members.json`, `publications.json`)을 무조건적으로 긁어와 로컬 캐시를 stale하게 덮어쓰던 문제를 우회했습니다. 데이터 로드 시 주소 뒤에 타임스탬프 쿼리 파라미터(`?t=Date.now()`)를 바인딩하여 브라우저의 HTTP 캐시를 무효화하고 실제 깃허브 서버의 최신본 데이터를 실시간으로 강제 fetch하도록 보장했습니다.

### 12. 메인 화면 통계 수치(Publications & Team Members) 동적 연동 및 구성원 수 기준 조정 [NEW]
- **실시간 통계값 반영**: 메인 화면 히어로 섹션 내의 통계 패널(Publications, Team Members)을 하드코딩된 값에서 탈피하고, `data/publications.json` 및 `data/members.json` 데이터베이스로부터 각각의 데이터 개수를 파싱하여 동적으로 계산해 표시하도록 구현했습니다.
- **구성원 수 집계 기준 조정**: "Team Members" 통계 항목 산출 시, 졸업생(`group: 'alumni'`)뿐만 아니라 사용자 요청에 맞춰 지도 교수님도 제외함으로써 순수 재학생 구성원 수(대학원생 6명 + 학부 인턴 3명 = 총 9명)만을 실시간 집계하도록 조정하였습니다.

---

## 검증 결과

1. **구성원 사진 업로드 및 로컬 서버 저장 검증**:
   - 관리자 모드 로그인(`midas123`) 후 연구원 추가/수정 모달창을 열었을 때, 기존 사진 또는 이니셜이 둥근 박스 안에 성공적으로 미리보기 렌더링됨을 확인했습니다.
   - 새로운 정방형 이미지 파일을 업로드 선택 시 모달창의 미리보기가 Live로 즉시 교체되는 것을 확인했습니다.
   - 'Save' 클릭 시 비동기 JSON base64 API 통신을 통해 로컬 개발 서버의 `images/people/[id].jpg` 경로에 덮어쓰기 완료되며, 브라우저의 연구원 목록에 버전 쿼리 파라미터를 통해 지연 없이 새 사진이 갱신되어 출력됨을 완벽하게 검증했습니다.
2. **구성원 이메일 토글 기능 검증**:
   - `index.html#people` 섹션에서 대학원생, 학부생, 졸업생 카드 밑에 메일 주소가 처음에 가려지고, 파란색 이메일 아이콘 버튼만 표시됨을 확인했습니다.
   - 메일 아이콘 클릭 시 이메일 텍스트(`yooonwoo0303@gmail.com` 등)가 부드럽게 페이드인하며 렌더링되고, 툴팁이 '이메일 가리기'로 변경됨을 검증했습니다.
   - 다시 클릭 시 이메일 주소가 가려지고 원상 복구됨을 검증했습니다.
   - 교수님 카드의 이메일(`parkh@cau.ac.kr`)은 사용자 요청에 따라 숨김 처리 없이 언제나 선명하게 노출되어 있는 상태를 유지합니다.
3. **Publications 화면 동작 검증**:
   - `#28` ~ `#1` 순번 배지가 연도 배지 영역에 세로 형태로 깨짐 없이 잘 표현됨을 검증했습니다.
   - 구성원 매칭을 통해 `Daehyun Kim`, `Jonghun Shin`, `Mansang Ha` 등의 저자 폰트가 자동으로 굵고 언더라인이 적용되는 것을 확인했습니다.
   - `†`, `*` 기호가 작게 위첨자로 올라가며 마우스 호버 시 정확한 툴팁이 렌더링됩니다.
4. **News Ticker 동작 검증**:
   - `posts.json` 데이터베이스에 존재하는 최신 Lab News 3개가 연동되어 메인 페이지 하단에 동적 롤링 배너로 슬라이드 롤업되는 것을 확인했습니다.
   - 3번째 뉴스까지 스크롤한 뒤 4번째 복제된 첫 번째 뉴스로 자연스럽게 무한 스크롤되는 트랜지션을 관찰했습니다.
5. **실시간 저장 반영 및 캐시 우회 검증**:
   - 깃허브 토큰 입력 후 원격 배포 홈페이지상에서 직접 논문/구성원/게시판 수정을 전송했을 때 GitHub API를 통해 성공적으로 Commit & Push 됨을 확인했습니다.
   - 데이터 저장 이후 2분의 유예 시간 동안 새로고침 시에도 브라우저 로컬 저장소 캐시를 참조하여 수정 내용이 일시 유지되는 것을 확인했습니다.
   - 2분이 경과하여 빌드가 끝난 이후에도 브라우저가 옛날 JSON 데이터를 캐시에서 꺼내오는 오작동이 없으며, `?t=[timestamp]` 쿼리에 의해 깃허브 Pages에 갓 빌드된 최신 JSON 데이터를 직접 Fetch해와 올바르게 원본으로 대체 및 영구 보존됨을 성공적으로 검증했습니다.
6. **메인 화면 통계 수치 실시간 검증**:
   - 논문 개수(38개)와 졸업생을 제외한 활동 중인 연구실 구성원 수(9명, 지도 교수 제외)가 메인 화면 히어로 영역 통계에 각각 `38+`, `9+`로 동적 반영 및 표시됨을 확인했습니다.
7. **Admin 인증**:
   - 게시판 작성/삭제 관리자 비밀번호: `midas123`통해 브라우저 수준에서 즉각 전환되도록 설계하였습니다.

### 7. Research 페이지 고유 과학/공학 SVG 아이콘 및 실물 이미지 개선
- **고유 과학적 SVG 아이콘**: Research 페이지의 4가지 주요 연구 카드에 들어가던 범용 아이콘들을 각 연구 내용과 정밀하게 부합하는 세련되고 고유한 과학적 SVG 아이콘으로 대체하였습니다 (전고체 배터리 번개 셀, 이온 다공성 결정 격자 구조, 브레인 네트워크 시냅스, ALD 증착 기판 시각 모델).
- **실물 연구 이미지 교체**: 기존의 어색하고 비현실적인 AI 생성 이미지들을 실제 연구 분야에 맞는 정교하고 신뢰감 높은 사진으로 교체하였습니다 (`solid_state_battery.jpg`, `multivalent_battery.png`, `machine_learning.jpg`, `semiconductor_process.jpg`).

### 8. Board(게시판) 탭 신설 및 프리미엄 CRUD 구현
- **동적 CRUD 및 외부 데이터 분리**: `localStorage`를 사용해 게시글 작성(Create), 보기(Read), 삭제(Delete) 기능을 추가하였습니다. 대규모 논문 데이터(`data/publications.json`)와 게시판 초기 데이터(`data/posts.json`)를 비동기 Fetch 방식으로 분리해 코드 관리가 최적화되었습니다.
- **다중 이미지 업로드 및 Canvas 압축**: 직접 이미지 파일을 드래그하여 다중 업로드할 수 있는 업로드 UI를 도입했으며, 브라우저 로컬 스토리지 5MB 제한을 넘지 않도록 Canvas API 기반 JPEG 0.7 압축을 통해 파일 크기를 경량화(100KB 내외) 시킵니다.
- **캐러셀 슬라이더 및 장수 배지**: 상세 모달창에서 다수 첨부 이미지를 인디케이터 도트와 함께 스와이프 가능한 슬라이더를 추가하였으며, 격자 목록에서는 첨부된 추가 이미지 장수를 세련된 반투명 배지(`+1`, `+2` 등)로 표기합니다.

### 9. 브레드크럼 분기 세분화 및 메뉴 자동 닫힘
- **LNB 브레드크럼 People 분기 세분화**: `board.html` 및 `publications.html` 페이지의 브레드크럼 LNB dropdown 내부의 single `People` 링크를 GNB 구조와 같이 `Professor`, `Members`, `Alumni` 3개 앵커 링크로 정교하게 세분화했습니다.
- **모바일 드로어 클릭 시 자동 닫힘**: 모바일 서랍 내비게이션 메뉴 클릭 시 서랍이 자동으로 딤 처리와 함께 부드럽게 접히도록 수정했습니다.

### 10. 데이터 베이스 관리 위생 청소
- **불필요 파일 제거**: 이전 버전의 잔재로 남아있거나 불필요해진 단독 탭 HTML 파일(`research.html`, `people.html`, `contact.html`)과 미사용 중복 정보로 혼선을 야기하던 `reconstructed_members.json` 및 기타 로그 추적용 텍스트 파일들을 모두 안전하게 정리하여 소스 코드의 신뢰도와 보전성을 높였습니다.

### 11. GitHub Pages 배포 캐시 우회 및 갱신 지연 완화 (Cache Busting) [NEW]
- **배포 및 빌드 지연 로컬 캐싱**: GitHub API를 통해 데이터베이스를 업데이트한 후, GitHub Actions 빌드가 수행되고 GitHub Pages 서버에 반영되기까지 약 1~2분의 지연이 발생합니다. 이 동안 브라우저 새로고침 시 변경 전의 서버 데이터를 다시 받아와 덮어씌워지는 현상을 방지하기 위해, 저장 시점 기준 2분 동안은 서버 패치를 유예하고 로컬 브라우저의 `localStorage` 캐시를 우선적으로 불러오도록 락(Lock) 기능을 구현했습니다.
- **브라우저 HTTP 캐시 버스팅**: 빌드가 완벽히 끝난 이후에도 브라우저가 디스크 캐시에 보관 중인 옛날 JSON 데이터베이스 파일(`posts.json`, `members.json`, `publications.json`)을 무조건적으로 긁어와 로컬 캐시를 stale하게 덮어쓰던 문제를 우회했습니다. 데이터 로드 시 주소 뒤에 타임스탬프 쿼리 파라미터(`?t=Date.now()`)를 바인딩하여 브라우저의 HTTP 캐시를 무효화하고 실제 깃허브 서버의 최신본 데이터를 실시간으로 강제 fetch하도록 보장했습니다.
- **HTML/JS 버전 파라미터 동기화 (v=47 업데이트)**: `board.html` 및 `post.html` 등에서 과거 버전(`v=35`)의 `app.js`를 요청하여 브라우저의 로컬 캐시 루프가 발생하는 버그를 수정했습니다. 모든 진입점 페이지(`index.html`, `publications.html`, `people.html`, `board.html`, `post.html`)의 `app.js` 호출 쿼리 파라미터를 `v=47`로 통일하고, `app.js` 내의 `CURRENT_VERSION`을 `47`로 업데이트하여 캐시 버전이 `44` 등으로 잘못 고착되거나 리다이렉트 루프를 도는 현상을 완전히 해결했습니다.


---

## 검증 결과

1. **구성원 사진 업로드 및 로컬 서버 저장 검증**:
   - 관리자 모드 로그인(`midas123`) 후 연구원 추가/수정 모달창을 열었을 때, 기존 사진 또는 이니셜이 둥근 박스 안에 성공적으로 미리보기 렌더링됨을 확인했습니다.
   - 새로운 정방형 이미지 파일을 업로드 선택 시 모달창의 미리보기가 Live로 즉시 교체되는 것을 확인했습니다.
   - 'Save' 클릭 시 비동기 JSON base64 API 통신을 통해 로컬 개발 서버의 `images/people/[id].jpg` 경로에 덮어쓰기 완료되며, 브라우저의 연구원 목록에 버전 쿼리 파라미터를 통해 지연 없이 새 사진이 갱신되어 출력됨을 완벽하게 검증했습니다.
2. **구성원 이메일 토글 기능 검증**:
   - `index.html#people` 섹션에서 대학원생, 학부생, 졸업생 카드 밑에 메일 주소가 처음에 가려지고, 파란색 이메일 아이콘 버튼만 표시됨을 확인했습니다.
   - 메일 아이콘 클릭 시 이메일 텍스트(`yooonwoo0303@gmail.com` 등)가 부드럽게 페이드인하며 렌더링되고, 툴팁이 '이메일 가리기'로 변경됨을 검증했습니다.
   - 다시 클릭 시 이메일 주소가 가려지고 원상 복구됨을 검증했습니다.
   - 교수님 카드의 이메일(`parkh@cau.ac.kr`)은 사용자 요청에 따라 숨김 처리 없이 언제나 선명하게 노출되어 있는 상태를 유지합니다.
3. **Publications 화면 동작 검증**:
   - `#28` ~ `#1` 순번 배지가 연도 배지 영역에 세로 형태로 깨짐 없이 잘 표현됨을 검증했습니다.
   - 구성원 매칭을 통해 `Daehyun Kim`, `Jonghun Shin`, `Mansang Ha` 등의 저자 폰트가 자동으로 굵고 언더라인이 적용되는 것을 확인했습니다.
   - `†`, `*` 기호가 작게 위첨자로 올라가며 마우스 호버 시 정확한 툴팁이 렌더링됩니다.
4. **News Ticker 동작 검증**:
   - `posts.json` 데이터베이스에 존재하는 최신 Lab News 3개가 연동되어 메인 페이지 하단에 동적 롤링 배너로 슬라이드 롤업되는 것을 확인했습니다.
   - 3번째 뉴스까지 스크롤한 뒤 4번째 복제된 첫 번째 뉴스로 자연스럽게 무한 스크롤되는 트랜지션을 관찰했습니다.
5. **실시간 저장 반영 및 캐시 우회 검증**:
   - 깃허브 토큰 입력 후 원격 배포 홈페이지상에서 직접 논문/구성원/게시판 수정을 전송했을 때 GitHub API를 통해 성공적으로 Commit & Push 됨을 확인했습니다.
   - 데이터 저장 이후 2분의 유예 시간 동안 새로고침 시에도 브라우저 로컬 저장소 캐시를 참조하여 수정 내용이 일시 유지되는 것을 확인했습니다.
   - 2분이 경과하여 빌드가 끝난 이후에도 브라우저가 옛날 JSON 데이터를 캐시에서 꺼내오는 오작동이 없으며, `?t=[timestamp]` 쿼리에 의해 깃허브 Pages에 갓 빌드된 최신 JSON 데이터를 직접 Fetch해와 올바르게 원본으로 대체 및 영구 보존됨을 성공적으로 검증했습니다.
6. **Admin 인증**:
   - 게시판 작성/삭제 관리자 비밀번호: `midas123`
